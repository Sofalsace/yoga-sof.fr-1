import { parseGaufresDataContent, formatEuro } from '../../src/components/contents/articles/sales';
import { SalesChart } from '../../src/components/contents/articles/SalesChart';
import { SalesDayTable } from '../../src/components/contents/articles/SalesDayTable';
import { SalesItemsChart } from '../../src/components/contents/articles/SalesItemsChart';
import { YogaImage } from '../../src/components/contents/yoga/YogaImage';
import { FrontsiteContent } from '../../src/components/layout/public/FrontsiteContent';
import gaufres2026 from  '../data/gaufres-2026.json';

export const meta = {
  title: 'Hésingue en fête 2025',
};

export const data = parseGaufresDataContent(gaufres2026);

export const sold2024 = 585;

<p/>

Le week-end du **5 au 6 septembre 2025** s'est tenue la cinquième édition de **[Hésingue en fête](https://www.lacometehesingue.fr/programmation/hesingue-en-fete)**. Tout comme [les années précédentes](/articles/hesingue-en-fete-2025), l'association **Yoga-Sof** était présente pour participer à l'animation du festival.

<YogaImage src="/images/gaufres-2026.jpg" alt="Stand de gaufres pour Hésingue en fête 2026" />

<p/>

**{data.totalSold}** gaufres/gâteaux ont été vendus pendant toute la durée de l'événement, pour un total de **{formatEuro(data.totalSoldEuro, false)}**. En tenant compte des frais engagés, les bénéfices réels pour l'association ont été de **{formatEuro(data.totalProfitsEuro, false)}**.
Pour mémoire, l'année dernière l'association avait vendu {sold2024} gaufres, soit une diminution de {(100 * (1 - data.totalSold / sold2024)).toFixed(0)} % cette année.

<SalesDayTable data={data.days} />

<p/>

_(\*) Marge d'erreur d'environ 5% (sous-estimation du nombre de ventes ; les recettes sont exactes)._

À titre d'information, les garnitures des gaufres suivaient la distribution suivante :

<SalesItemsChart data={data} />

<p/>

Enfin, voici le nombre total de ventes, heure par heure :

<SalesChart data={data.days} />

Vous pouvez retrouver les données brutes [ici](https://github.com/FlorianCassayre/yoga-sof.fr/tree/master/contents/data/gaufres-2026.json).

<p/>

**Au nom de l'association, je souhaite remercier chaleureusement tous les membres qui ont investi de leur temps pour la réussite de cet événement.** Et évidemment, aux habitants qui nous ont généreusement soutenu.

_Un événement interne sera organisé avec les participants du stand._

export default ({ children }) => <FrontsiteContent {...meta}>{children}</FrontsiteContent>
